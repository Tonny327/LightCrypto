# Портативность исполняемого файла LightCrypto

## ✅ Что включено в исполняемый файл

Исполняемый файл `lightcrypto` является **самодостаточным** и включает:

1. **Все Python модули приложения** - весь код GUI
2. **Все библиотеки Python** - PyQt6 и все зависимости
3. **Исполняемые файлы проекта:**
   - `tap_encrypt` (из `build/`)
   - `tap_decrypt` (из `build/`)
4. **CSV файлы с ключами шифрования:**
   - Все файлы из директории `CipherKeys/` (15 файлов)
5. **Скрипты настройки TAP интерфейсов:**
   - `setup_tap_A.sh`
   - `setup_tap_B.sh`
   - `setup_tap_pair.sh`

## 🚀 Запуск из любой директории

### Способ 1: Полный путь
```bash
/home/tonny/projects/MyCryptoProject/LightCrypto/gui_qt/dist/lightcrypto
```

### Способ 2: Относительный путь
```bash
cd /home/tonny/projects/MyCryptoProject/LightCrypto/gui_qt/dist
./lightcrypto
```

### Способ 3: Добавить в PATH (для удобства)
```bash
# Временно (для текущей сессии)
export PATH=$PATH:/home/tonny/projects/MyCryptoProject/LightCrypto/gui_qt/dist
lightcrypto

# Постоянно (добавить в ~/.bashrc)
echo 'export PATH=$PATH:/home/tonny/projects/MyCryptoProject/LightCrypto/gui_qt/dist' >> ~/.bashrc
source ~/.bashrc
```

### Способ 4: Создать символическую ссылку
```bash
sudo ln -s /home/tonny/projects/MyCryptoProject/LightCrypto/gui_qt/dist/lightcrypto /usr/local/bin/lightcrypto
lightcrypto  # Теперь можно запускать откуда угодно
```

## 📦 Что НЕ нужно

- ❌ Python (включен в исполняемый файл)
- ❌ PyQt6 (включен в исполняемый файл)
- ❌ Дополнительные Python пакеты (все включено)
- ❌ CSV файлы из CipherKeys (включены)
- ❌ Исполняемые файлы tap_encrypt/tap_decrypt (включены)
- ❌ Скрипты setup (включены)

## ⚠️ Что МОЖЕТ потребоваться

### Системные библиотеки Qt (на целевой машине)

На **другом Linux компьютере** могут потребоваться системные библиотеки Qt:

```bash
# Ubuntu/Debian
sudo apt-get install libxcb-cursor0 libxcb-xinerama0 libqt6gui6 libqt6core6 libqt6widgets6

# Fedora/RHEL
sudo dnf install qt6-qtbase-gui

# Arch Linux
sudo pacman -S qt6-base
```

**Примечание:** На вашем компьютере эти библиотеки уже установлены, поэтому приложение работает.

## 🔍 Как работает PyInstaller (onefile режим)

1. **При запуске:** Исполняемый файл распаковывает все включенные файлы во временную директорию `/tmp/_MEIxxxxxx/`
2. **Во время работы:** Приложение использует распакованные файлы
3. **После закрытия:** Временные файлы автоматически удаляются

**Важно:** При первом запуске может потребоваться немного больше времени на распаковку.

## 📋 Проверка включенных файлов

Вы можете проверить, что файлы включены:

```bash
strings lightcrypto | grep -E "tap_encrypt|CipherKeys|setup_tap" | head -20
```

## 🌍 Перенос на другой компьютер

1. **Скопируйте файл:**
   ```bash
   scp gui_qt/dist/lightcrypto user@remote:/path/to/destination/
   ```

2. **Установите системные библиотеки Qt** (если нужно):
   ```bash
   sudo apt-get install libxcb-cursor0 libxcb-xinerama0 libqt6gui6 libqt6core6 libqt6widgets6
   ```

3. **Запустите:**
   ```bash
   chmod +x lightcrypto
   ./lightcrypto
   ```

## ✅ Итог

**Да, файл полностью самодостаточный!** Единственное, что может потребоваться на другом компьютере - это системные библиотеки Qt (если они не установлены). Все остальное включено в исполняемый файл.

