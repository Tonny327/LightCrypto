# 🔐 LightCrypto GUI

Упрощенный графический интерфейс для криптографической системы LightCrypto со встроенным терминалом внутри окна GUI.

## 🚀 Быстрый запуск

```bash
# 1. Настройка sudo доступа (один раз)
bash setup_sudo.sh

# 2. Запуск GUI приложений
python3 decrypt_gui.py &    # Интерфейс расшифровки (в фоне)
python3 encrypt_gui.py      # Интерфейс шифрования
```

## 📁 Файлы GUI

| Файл | Описание |
|------|----------|
| `encrypt_gui.py` | GUI для шифрования (неймспейс ns1) |
| `decrypt_gui.py` | GUI для расшифровки (неймспейс ns2) |
| `setup_sudo.sh` | Настройка sudo доступа без пароля |
| `test_gui.py` | Тестирование GUI модулей |
| `README_SETUP.md` | Подробная инструкция |

## 🖥️ Интерфейс

- **Верхняя панель**: Кнопка запуска + настройки (IP, порт, режим сообщений)
- **Встроенный терминал**: Черный фон, зеленый текст, полный вывод программ
- **Поле ввода команд**: Можно вводить команды прямо в терминале
- **Кнопка очистки**: "Очистить" для очистки терминала
- **Размер окна**: 800x700 для удобного просмотра терминала

### 📺 Что происходит при нажатии "Запустить задачу":

**Во встроенном терминале внутри GUI:**
```
🖥️ LightCrypto встроенный терминал
💡 Введите команду или используйте кнопку 'Запустить задачу'

🔍 Проверка sudo доступа...
✅ Sudo доступ настроен корректно
🌐 Доступные неймспейсы:
   ns1 (id: 0)
   ns2 (id: 1)

🚀 Запуск LightCrypto Encrypt...
📝 Команда: sudo ip netns exec ns1 ./build/tap_encrypt 192.168.1.2 12345

🌐 Используем IP: 192.168.1.2, порт: 12345
✅ IP-адрес 192.168.1.2 доступен, начинаем работу...
📡 tap0 открыт для чтения Ethernet-кадров
📤 Публичный ключ отправлен получателю
📥 Публичный ключ получен от получателя
📤 Отправлен зашифрованный кадр (64 байт)
📤 Отправлен зашифрованный кадр (96 байт)
...

$ _  ← Можно вводить команды здесь
```

## 🔧 Команды

### encrypt_gui.py запускает:
```bash
sudo ip netns exec ns1 ./build/tap_encrypt [--msg] IP PORT
```

### decrypt_gui.py запускает:
```bash
sudo ip netns exec ns2 ./build/tap_decrypt [--msg] [IP] PORT
```

## 📋 Устранение проблем

| Проблема | Решение |
|----------|---------|
| GUI запрашивает пароль | `bash setup_sudo.sh` |
| Файл не найден | `cd build && cmake .. && make` |
| Неймспейсы не найдены | См. `README_SETUP.md` |

## ✨ Особенности

### 🖥️ Встроенный терминал
- **Внутри GUI**: Терминал встроен прямо в окно приложения
- **Полный вывод**: Показывает весь вывод программ tap_encrypt/tap_decrypt
- **Интерактивность**: Поле ввода команд внизу терминала
- **Черный фон, зеленый текст**: Классический вид Linux терминала

### 🔧 Управление процессами
- **Простое управление**: Одна кнопка для запуска/остановки в верхней панели
- **PTY эмуляция**: Использует псевдотерминал для полного захвата вывода
- **Корректное завершение**: SIGTERM → SIGKILL для надежного закрытия
- **Автосброс состояния**: Кнопки сбрасываются при завершении процесса

### 📡 Дополнительные возможности
- **Ввод команд**: Можно вводить любые команды прямо в терминале
- **Очистка терминала**: Кнопка "Очистить" для очистки вывода
- **Диагностика**: Проверка sudo доступа и неймспейсов при запуске
- **Сообщения об ошибках**: Подробная информация о проблемах

### 🎨 Компактный интерфейс
- **Верхняя панель**: Все настройки и кнопки в одной строке
- **Большой терминал**: Максимум места для вывода программ
- **Удобный размер**: 800x700 для комфортной работы

## 🎯 Готово к использованию!

Все файлы созданы и настроены. Запускайте GUI и наслаждайтесь защищенной передачей данных! 🚀
