# 🔐 LightCrypto GUI

Упрощенный графический интерфейс для криптографической системы LightCrypto.

## 🚀 Быстрый запуск

```bash
# 1. Настройка sudo доступа (один раз)
bash setup_sudo.sh

# 2. Запуск GUI приложений
python3 decrypt_gui.py &    # Интерфейс расшифровки (в фоне)
python3 encrypt_gui.py      # Интерфейс шифрования
```

## 📁 Файлы GUI

| Файл | Описание |
|------|----------|
| `encrypt_gui.py` | GUI для шифрования (неймспейс ns1) |
| `decrypt_gui.py` | GUI для расшифровки (неймспейс ns2) |
| `setup_sudo.sh` | Настройка sudo доступа без пароля |
| `test_gui.py` | Тестирование GUI модулей |
| `README_SETUP.md` | Подробная инструкция |

## 🖥️ Интерфейс

- **Одна кнопка**: "Запустить задачу" / "Остановить задачу"
- **Консоль**: Белый фон, черный текст, **реальный вывод tap_encrypt/tap_decrypt**
- **Настройки**: IP адрес, порт, режим текстовых сообщений

### 📺 Что вы увидите в консоли:

**encrypt_gui.py:**
```
🌐 Используем IP: 192.168.1.2, порт: 12345
✅ IP-адрес 192.168.1.2 доступен, начинаем работу...
📡 tap0 открыт для чтения Ethernet-кадров
📤 Публичный ключ отправлен получателю
📥 Публичный ключ получен от получателя
📤 Отправлен зашифрованный кадр (64 байт)
📤 Отправлен зашифрованный кадр (96 байт)
```

**decrypt_gui.py:**
```
🌐 Ожидаем пакеты на IP: 0.0.0.0, порт: 12345
📡 tap1 открыт для записи расшифрованных Ethernet-кадров
📥 Публичный ключ отправителя получен
📤 Отправлен свой публичный ключ отправителю
✅ Принят и расшифрован кадр (64 байт)
✅ Хеши совпадают — кадр корректен
```

## 🔧 Команды

### encrypt_gui.py запускает:
```bash
sudo ip netns exec ns1 ./build/tap_encrypt [--msg] IP PORT
```

### decrypt_gui.py запускает:
```bash
sudo ip netns exec ns2 ./build/tap_decrypt [--msg] [IP] PORT
```

## 📋 Устранение проблем

| Проблема | Решение |
|----------|---------|
| GUI запрашивает пароль | `bash setup_sudo.sh` |
| Файл не найден | `cd build && cmake .. && make` |
| Неймспейсы не найдены | См. `README_SETUP.md` |

## ✨ Особенности

- **Реальный вывод**: GUI показывает тот же вывод, что и консольные tap_encrypt/tap_decrypt
- **PTY эмуляция**: Использует псевдотерминал для захвата вывода в реальном времени
- **Все эмодзи**: Сохранены все эмодзи и форматирование из оригинальных программ
- **Немедленный вывод**: Сообщения появляются мгновенно без буферизации
- **Размеры кадров**: Видно сколько байт шифруется/расшифровывается
- **Статус операций**: Отображается успешность операций и проверка хешей

## 🎯 Готово к использованию!

Все файлы созданы и настроены. Запускайте GUI и наслаждайтесь защищенной передачей данных! 🚀
