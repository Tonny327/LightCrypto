# 📁 Руководство по передаче файлов через LightCrypto

## 🚀 Быстрый запуск

### Автоматический тест
```bash
# Тест с libsodium
./test_file_transfer.sh libsodium

# Тест с кастомным кодеком
./test_file_transfer.sh codec
```

---

## 📋 Алгоритм 1: LibSodium (ChaCha20-Poly1305)

### Характеристики
- **Шифрование:** ChaCha20-Poly1305 (AEAD)
- **Обмен ключами:** X25519 (автоматический)
- **Скорость:** ~110 КБ/с
- **Рекомендуется:** Для большинства случаев

### Запуск

**ШАГ 1 - Получатель (запустить первым):**
```bash
./build/tap_decrypt --file --output received_file.ext [ПОРТ]
```

**ШАГ 2 - Отправитель:**
```bash
./build/tap_encrypt --file your_file.ext IP_АДРЕС ПОРТ
```

### Параметры

| Параметр | Описание | Обязательный | По умолчанию |
|----------|----------|--------------|--------------|
| `--file` | Режим передачи/приёма файла | ✅ Да | - |
| `--output` | Путь сохранения (только получатель) | ❌ Нет | ./оригинальное_имя |
| `IP_АДРЕС` | IP получателя (только отправитель) | ✅ Да | - |
| `ПОРТ` | UDP порт | ✅ Да | 12345 |

### Примеры

#### Пример 1: Локальная передача
```bash
# Терминал 1 (получатель)
./build/tap_decrypt --file --output photo.jpg 12345

# Терминал 2 (отправитель)
./build/tap_encrypt --file test_image.png 127.0.0.1 12345
```

#### Пример 2: Между двумя компьютерами
```bash
# Компьютер B (192.168.1.100) - получатель
./build/tap_decrypt --file --output document.pdf 12345

# Компьютер A - отправитель
./build/tap_encrypt --file report.pdf 192.168.1.100 12345
```

#### Пример 3: Автоматическое имя файла
```bash
# Получатель (файл сохранится с оригинальным именем в текущей папке)
./build/tap_decrypt --file 12345

# Отправитель
./build/tap_encrypt --file vacation_photo.jpg 192.168.1.100 12345
# Результат: ./vacation_photo.jpg
```

---

## 🎛️ Алгоритм 2: Custom Digital Codec

### Характеристики
- **Кодирование:** Параметризируемый цифровой кодек
- **Обмен ключами:** Не требуется (синхронизация параметров)
- **Скорость:** ~66 КБ/с (медленнее)
- **Применение:** Для исследований и экспериментов

### Запуск

**ШАГ 1 - Получатель (запустить первым):**
```bash
./build/tap_decrypt --file --output received_file.ext \
    --codec ПУТЬ_К_CSV \
    --M БИТЫ_M \
    --Q БИТЫ_Q \
    --fun ТИП_ФУНКЦИИ \
    --h1 СОСТОЯНИЕ_1 \
    --h2 СОСТОЯНИЕ_2 \
    [ПОРТ]
```

**ШАГ 2 - Отправитель:**
```bash
./build/tap_encrypt --file your_file.ext \
    --codec ПУТЬ_К_CSV \
    --M БИТЫ_M \
    --Q БИТЫ_Q \
    --fun ТИП_ФУНКЦИИ \
    --h1 СОСТОЯНИЕ_1 \
    --h2 СОСТОЯНИЕ_2 \
    IP_АДРЕС ПОРТ
```

### Параметры кодека

| Параметр | Описание | Диапазон | По умолчанию |
|----------|----------|----------|--------------|
| `--codec` | Путь к CSV файлу с коэффициентами | - | ❌ Обязательно |
| `--M` | Размер слова (бит) | 1-31 | 8 |
| `--Q` | Информационные биты на символ | 1-M | 6 |
| `--fun` | Тип функции кодирования | 1-5 | 1 |
| `--h1` | Начальное состояние генератора 1 | 0-255 | 7 |
| `--h2` | Начальное состояние генератора 2 | 0-255 | 23 |

### ⚠️ ВАЖНО: Параметры должны ПОЛНОСТЬЮ совпадать на обоих устройствах!

### Доступные CSV файлы коэффициентов

```
CipherKeys/
├── Q=2.csv
├── Q=2_2.csv
├── Q=2_3.csv
├── Q=2_4.csv          # Рекомендуется (M=8, Q=6)
├── Q=2_5.csv
├── Q=2_6.csv
├── Q=2_7.csv          # Повышенная безопасность (M=8, Q=7)
├── Q=2_8.csv
├── Q=2_ax2_by_q.csv
├── Q=4.csv
├── QDM-6.csv
├── QDM-6, Q = 6, tol = 20.csv
├── N_16_6_primes.csv
└── N_16_7.csv
```

### Примеры с кодеком

#### Пример 1: Базовая конфигурация (M=8, Q=6)
```bash
# Получатель
./build/tap_decrypt --file --output secret.dat \
    --codec CipherKeys/Q=2_4.csv \
    --M 8 --Q 6 \
    12345

# Отправитель
./build/tap_encrypt --file confidential.dat \
    --codec CipherKeys/Q=2_4.csv \
    --M 8 --Q 6 \
    192.168.1.100 12345
```

#### Пример 2: Повышенная безопасность (M=8, Q=7)
```bash
# Получатель
./build/tap_decrypt --file --output ultra_secret.pdf \
    --codec CipherKeys/Q=2_7.csv \
    --M 8 --Q 7 \
    12345

# Отправитель
./build/tap_encrypt --file top_secret.pdf \
    --codec CipherKeys/Q=2_7.csv \
    --M 8 --Q 7 \
    192.168.1.100 12345
```

#### Пример 3: С пользовательскими начальными состояниями
```bash
# Получатель
./build/tap_decrypt --file --output data.bin \
    --codec CipherKeys/Q=2_5.csv \
    --M 8 --Q 6 \
    --fun 1 \
    --h1 137 \
    --h2 219 \
    12345

# Отправитель
./build/tap_encrypt --file source.bin \
    --codec CipherKeys/Q=2_5.csv \
    --M 8 --Q 6 \
    --fun 1 \
    --h1 137 \
    --h2 219 \
    192.168.1.100 12345
```

#### Пример 4: Альтернативная функция кодирования
```bash
# Получатель
./build/tap_decrypt --file --output file.dat \
    --codec CipherKeys/Q=2_6.csv \
    --M 8 --Q 6 \
    --fun 3 \
    12345

# Отправитель  
./build/tap_encrypt --file file.dat \
    --codec CipherKeys/Q=2_6.csv \
    --M 8 --Q 6 \
    --fun 3 \
    192.168.1.100 12345
```

---

## 📊 Сравнение алгоритмов

| Параметр | LibSodium | Custom Codec |
|----------|-----------|--------------|
| **Скорость** | ~110 КБ/с | ~66 КБ/с |
| **Безопасность** | Криптографически стойкое | Экспериментальное |
| **Настройка** | Не требуется | Много параметров |
| **Обмен ключами** | Автоматический | Не нужен |
| **Сложность** | Простая | Требует понимания |
| **Оверхед данных** | +10% | +30-50% |
| **Применение** | Продуктивное использование | Исследования |

---

## 🔧 Где сохраняется файл

### Без параметра `--output`
Файл сохраняется в **текущую директорию** с **оригинальным именем**:
```bash
./build/tap_decrypt --file 12345
# Результат: ./оригинальное_имя_файла.ext
```

### С параметром `--output`
Файл сохраняется **по указанному пути**:
```bash
./build/tap_decrypt --file --output /path/to/file.ext 12345
# Результат: /path/to/file.ext
```

---

## ✅ Проверка целостности

После передачи автоматически проверяются:
- ✅ **CRC32** для каждого чанка
- ✅ **SHA-256** для всего файла

При совпадении хешей:
```
✅ Все чанки получены, сохраняем файл...
✅ Файл успешно сохранен: ./filename.ext
✅ Проверка целостности пройдена!
```

Дополнительная проверка вручную:
```bash
# Сравнить хеши
sha256sum original_file.dat received_file.dat

# Сравнить размеры
ls -lh original_file.dat received_file.dat
```

---

## ⚠️ Важные замечания

### Порядок запуска
1. **Сначала получатель** (tap_decrypt)
2. **Потом отправитель** (tap_encrypt)

### Совпадение параметров
Для **Custom Codec** все параметры должны быть **идентичны**:
- ✅ `--codec` - один и тот же CSV файл
- ✅ `--M` - одинаковое значение
- ✅ `--Q` - одинаковое значение
- ✅ `--fun` - одинаковое значение
- ✅ `--h1` - одинаковое значение
- ✅ `--h2` - одинаковое значение

### Сеть
- Устройства должны быть в одной сети или иметь маршрут
- UDP порт должен быть открыт в брандмауэре
- Проверка доступности: `ping IP_АДРЕС`

---

## 🐛 Решение проблем

### Ошибка: "Не удалось открыть файл"
```bash
# Проверьте путь к файлу
ls -l your_file.ext

# Проверьте права доступа
chmod 644 your_file.ext
```

### Ошибка: "Хеш не совпадает"
- Проверьте параметры кодека (должны совпадать)
- Повторите передачу
- Проверьте стабильность сети

### Ошибка: "Connection refused"
```bash
# Проверьте, что получатель запущен
ps aux | grep tap_decrypt

# Проверьте сеть
ping IP_АДРЕС
```

### Передача слишком медленная
- Используйте libsodium вместо codec
- Проверьте загрузку сети
- Используйте проводное соединение

---

## 📝 Шпаргалка команд

### LibSodium - локально
```bash
# Получатель
./build/tap_decrypt --file --output received.dat 12345

# Отправитель
./build/tap_encrypt --file original.dat 127.0.0.1 12345
```

### LibSodium - по сети
```bash
# Получатель (на 192.168.1.100)
./build/tap_decrypt --file --output file.pdf 12345

# Отправитель
./build/tap_encrypt --file document.pdf 192.168.1.100 12345
```

### Codec - базовая конфигурация
```bash
# Получатель
./build/tap_decrypt --file --output file.dat \
    --codec CipherKeys/Q=2_4.csv --M 8 --Q 6 12345

# Отправитель
./build/tap_encrypt --file file.dat \
    --codec CipherKeys/Q=2_4.csv --M 8 --Q 6 192.168.1.100 12345
```

### Codec - максимальная безопасность
```bash
# Получатель
./build/tap_decrypt --file --output secret.dat \
    --codec CipherKeys/Q=2_7.csv --M 8 --Q 7 \
    --fun 1 --h1 137 --h2 219 12345

# Отправитель
./build/tap_encrypt --file secret.dat \
    --codec CipherKeys/Q=2_7.csv --M 8 --Q 7 \
    --fun 1 --h1 137 --h2 219 192.168.1.100 12345
```

---

## 🎓 Рекомендации

### Когда использовать LibSodium
- ✅ Повседневное использование
- ✅ Нужна максимальная скорость
- ✅ Требуется криптографическая стойкость
- ✅ Простота важнее гибкости

### Когда использовать Custom Codec
- 🧪 Научные исследования
- 🔬 Изучение алгоритмов кодирования
- 🎓 Образовательные цели
- ⚙️ Нужна тонкая настройка параметров

---

**Компиляция проекта:**
```bash
cd build
cmake ..
make
```

**Создание тестового файла:**
```bash
python3 create_test_image.py test_image.png
```

