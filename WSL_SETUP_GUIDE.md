# 🚀 LightCrypto - Руководство по запуску через WSL 2

> **Версия с оптимизацией для WSL 2 (UDP-ready)**

## ✅ Что изменено в этой версии:

- **tap_encrypt.cpp**: Добавлен `connect()` для UDP сокета - обеспечивает надежную работу recv()/send()
- **tap_decrypt.cpp**: Улучшенное логирование для отладки WSL подключений
- Оптимизирована работа UDP через NAT WSL 2

---

## 📋 Требования

**На обоих устройствах:**
- Windows 10/11 с WSL 2
- Ubuntu в WSL
- Оба устройства в одной локальной сети

---

## 🛠 Подготовка (на обоих устройствах)

### Шаг 1: Проверьте версию WSL

```powershell
wsl --list --verbose
```

Должно показать `VERSION = 2`. Если нет:

```powershell
wsl --set-version Ubuntu 2
```

### Шаг 2: Установите зависимости в WSL

```bash
sudo apt update
sudo apt install -y libsodium-dev build-essential iproute2 iputils-ping git
```

### Шаг 3: Скомпилируйте проект

```bash
cd ~/LightCrypto
mkdir -p build

# Компиляция с новыми изменениями для WSL
g++ -o build/tap_encrypt src/tap_encrypt.cpp src/digital_codec.cpp -lsodium -pthread
g++ -o build/tap_decrypt src/tap_decrypt.cpp src/digital_codec.cpp -lsodium -pthread

# Проверка
ls -lh build/
```

---

## 🌐 Настройка сети

### Определите IP адреса

**На устройстве B (приёмник) в WSL:**
```bash
hostname -I
```
Запомните первый IP (например, `172.26.43.251`)

**В Windows PowerShell на устройстве B:**
```powershell
ipconfig
```
Найдите IP вашего сетевого адаптера (например, `192.168.100.101`)

---

## 🚀 Запуск на двух устройствах

### **Устройство B (ПРИЁМНИК) - Запускаем ПЕРВЫМ**

#### 1. Настройте брандмауэр Windows (PowerShell от Администратора)

```powershell
New-NetFirewallRule -DisplayName "LightCrypto UDP" `
                    -Direction Inbound `
                    -Action Allow `
                    -Protocol UDP `
                    -LocalPort 12346 `
                    -Profile Any
```

#### 2. Создайте TAP-интерфейс в WSL

```bash
sudo ip tuntap add dev tap1 mode tap
sudo ip addr add 10.0.0.2/24 dev tap1
sudo ip link set tap1 up
sudo sysctl -w net.ipv6.conf.tap1.disable_ipv6=1

# Проверка
ip addr show tap1
```

#### 3. Запустите приёмник

```bash
cd ~/LightCrypto
sudo ./build/tap_decrypt 0.0.0.0 12346
```

**Ожидаемый вывод:**
```
✅ Сокет привязан к 0.0.0.0:12346 (WSL-ready mode)
📡 tap1 открыт для записи расшифрованных Ethernet-кадров
```

Программа будет ждать подключения.

---

### **Устройство A (ОТПРАВИТЕЛЬ) - Запускаем ВТОРЫМ**

#### 1. Создайте TAP-интерфейс в WSL

```bash
sudo ip tuntap add dev tap0 mode tap
sudo ip addr add 10.0.0.1/24 dev tap0
sudo ip link set tap0 up
sudo sysctl -w net.ipv6.conf.tap0.disable_ipv6=1

# Проверка
ip addr show tap0
```

#### 2. Узнайте IP устройства B

**ВАЖНО:** Используйте **IP WSL** устройства B, а не Windows IP!

На устройстве B выполните в WSL:
```bash
hostname -I
```

Например: `172.26.43.251`

#### 3. Проверьте связность

```bash
# Попробуйте пинговать IP WSL устройства B
ping 172.26.43.251 -c 3
```

**Если ping НЕ работает:**
- Устройства в разных подсетях
- Нужен другой подход (см. "Решение проблем" ниже)

**Если ping работает:**
- Отлично! Переходите к следующему шагу

#### 4. Запустите отправитель

```bash
cd ~/LightCrypto
sudo ./build/tap_encrypt 172.26.43.251 12346
```

**Замените `172.26.43.251` на реальный IP WSL устройства B!**

**Ожидаемый вывод:**
```
✅ UDP сокет подключён к 172.26.43.251:12346
📡 tap0 открыт для чтения Ethernet-кадров
📤 Публичный ключ отправлен получателю
📥 Публичный ключ получен от получателя
🔄 Двунаправленная передача включена
```

**На устройстве B должно появиться:**
```
📥 Публичный ключ отправителя получен от 172.X.X.X:XXXXX
📤 Отправлен свой публичный ключ отправителю
🔄 Двунаправленная передача включена
```

---

## ✅ Тестирование

На **устройстве A** откройте новый терминал WSL:

```bash
ping 10.0.0.2 -c 5
```

**Ожидаемый результат:**
```
PING 10.0.0.2 (10.0.0.2) 56(84) bytes of data.
64 bytes from 10.0.0.2: icmp_seq=1 ttl=64 time=5.42 ms
64 bytes from 10.0.0.2: icmp_seq=2 ttl=64 time=3.21 ms
```

**В терминалах программ появится:**
```
📤 Отправлен зашифрованный кадр (98 байт)
📥 Принят и расшифрован кадр (98 байт)
✅ Хеши совпадают — кадр корректен
```

🎉 **Поздравляю! Зашифрованный туннель работает!**

---

## 🛑 Остановка

```bash
# Нажмите Ctrl+C в терминалах программ

# Удалите TAP-интерфейсы
sudo ip link delete tap0  # на отправителе
sudo ip link delete tap1  # на приёмнике
```

---

## ❓ Решение проблем

### Проблема 1: Ping между WSL не работает

IP WSL устройства B недоступен из WSL устройства A из-за NAT.

**Решение: Используйте Windows IP с перенаправлением**

На устройстве B создайте UDP relay (Python скрипт):

```python
# Сохраните как udp_relay.py в Windows
import socket

WSL_IP = "172.26.43.251"  # IP WSL из hostname -I
WIN_PORT = 12346
WSL_PORT = 12346

win_sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
win_sock.bind(('0.0.0.0', WIN_PORT))

wsl_sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)

print(f"UDP Relay: 0.0.0.0:{WIN_PORT} -> {WSL_IP}:{WSL_PORT}")

while True:
    data, addr = win_sock.recvfrom(65536)
    print(f"[→WSL] {len(data)}B from {addr}")
    wsl_sock.sendto(data, (WSL_IP, WSL_PORT))
    
    try:
        wsl_sock.settimeout(5)
        resp, _ = wsl_sock.recvfrom(65536)
        win_sock.sendto(resp, addr)
        print(f"[←Client] {len(resp)}B to {addr}")
    except:
        pass
```

Запустите в PowerShell:
```powershell
python C:\udp_relay.py
```

Теперь с устройства A подключайтесь к **Windows IP** устройства B:
```bash
sudo ./build/tap_encrypt 192.168.100.101 12346
```

### Проблема 2: "Ошибка при получении публичного ключа"

Программы не могут обменяться ключами.

**Проверьте:**

1. **Брандмауэр на устройстве B:**
   ```powershell
   Get-NetFirewallRule -DisplayName "LightCrypto*"
   ```

2. **Программа слушает на порту:**
   ```bash
   sudo lsof -i UDP:12346
   ```

3. **Тест UDP связности:**
   ```bash
   # На устройстве A
   echo "test" | nc -u <IP_устройства_B> 12346
   ```

### Проблема 3: TAP-интерфейс не создаётся

```bash
# Проверьте /dev/net/tun
ls -l /dev/net/tun

# Если нет, создайте:
sudo mkdir -p /dev/net
sudo mknod /dev/net/tun c 10 200
sudo chmod 666 /dev/net/tun
```

Это работает только в WSL 2!

---

## 📊 Производительность

Тест с iperf:

**На устройстве B:**
```bash
iperf -s -B 10.0.0.2
```

**На устройстве A:**
```bash
iperf -c 10.0.0.2 -t 10
```

Типичная скорость: 50-150 Мбит/с (зависит от CPU и сети)

---

## 🔒 Безопасность

- ✅ Шифрование: ChaCha20-Poly1305 (AEAD)
- ✅ Обмен ключами: ECDH (Curve25519)
- ✅ Проверка целостности: SHA-256

---

## 💡 Советы

1. **Статический IP для WSL:** Настройте статический IP в WSL для стабильной работы
2. **Автозапуск:** Используйте скрипты из `scripts/` для автоматизации
3. **Мониторинг:** Используйте `tcpdump` для отладки:
   ```bash
   sudo tcpdump -i any -n 'udp port 12346'
   ```

---

## 📚 Дополнительные материалы

- **GUI версия:** См. `gui/` для графического интерфейса
- **Сетевые неймспейсы:** См. `README.md` для тестирования на одном устройстве
- **Custom Codec:** См. документацию по помехоустойчивому кодированию

---

**Версия документа:** 2.0 (WSL-optimized)
**Дата:** Декабрь 2024

