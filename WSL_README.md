# 🔐 LightCrypto - WSL Branch

> **Оптимизированная версия для работы через WSL 2 на Windows**

## 🎯 Что нового в этой ветке:

### ✅ Изменения в коде:

1. **tap_encrypt.cpp**:
   - Добавлен `connect()` для UDP сокета
   - Обеспечивает надёжную работу `recv()`/`send()` через WSL NAT
   - Улучшенное логирование соединения

2. **tap_decrypt.cpp**:
   - Оптимизирована работа с `recvfrom()`/`sendto()`
   - Добавлено логирование адреса отправителя для отладки
   - WSL-ready режим по умолчанию

### 📁 Новые файлы:

- `WSL_SETUP_GUIDE.md` - Подробная инструкция по настройке и запуску
- `wsl_start_receiver.sh` - Автоматический запуск приёмника
- `wsl_start_sender.sh` - Автоматический запуск отправителя  
- `udp_relay.py` - UDP relay для Windows (если прямое соединение невозможно)
- `WSL_README.md` - Этот файл

---

## 🚀 Быстрый старт

### Компиляция (на обоих устройствах):

```bash
cd ~/LightCrypto
mkdir -p build
g++ -o build/tap_encrypt src/tap_encrypt.cpp src/digital_codec.cpp -lsodium -pthread
g++ -o build/tap_decrypt src/tap_decrypt.cpp src/digital_codec.cpp -lsodium -pthread
```

### Запуск приёмника (Устройство B):

```bash
# Сделать скрипт исполняемым (первый раз)
chmod +x wsl_start_receiver.sh

# Запустить
sudo ./wsl_start_receiver.sh
```

### Запуск отправителя (Устройство A):

```bash
# Сделать скрипт исполняемым (первый раз)
chmod +x wsl_start_sender.sh

# Узнать IP WSL устройства B
# (на устройстве B выполните: hostname -I)

# Запустить с IP устройства B
sudo ./wsl_start_sender.sh 172.26.43.251
```

### Тест:

```bash
ping 10.0.0.2
```

---

## 📖 Полная документация

См. **`WSL_SETUP_GUIDE.md`** для:
- Детальной настройки
- Решения проблем
- Альтернативных методов подключения
- Использования UDP relay

---

## 🔧 Основные отличия от основной ветки

| Параметр | Основная ветка | WSL ветка |
|----------|----------------|-----------|
| **Целевая платформа** | Нативный Linux | WSL 2 на Windows |
| **UDP сокеты** | Стандартные | Оптимизированные для WSL NAT |
| **Логирование** | Базовое | Расширенное (для отладки WSL) |
| **Скрипты запуска** | `start_*.sh` | `wsl_start_*.sh` |
| **UDP Relay** | Не требуется | Опционально (udp_relay.py) |

---

## ⚠️ Известные ограничения WSL 2

1. **NAT между машинами**: WSL IP недоступен напрямую из другой сети
   - **Решение**: Используйте UDP relay или подключайтесь к WSL IP напрямую

2. **IP адрес меняется**: После перезапуска WSL IP может измениться
   - **Решение**: Проверяйте IP через `hostname -I` перед каждым запуском

3. **Производительность**: Немного ниже чем нативный Linux
   - **Ожидается**: 50-150 Мбит/с (зависит от CPU)

---

## 💡 Советы по использованию

### Вариант 1: Прямое подключение (если WSL IP доступен)

```bash
# На устройстве A
ping 172.26.43.251  # IP WSL устройства B

# Если работает:
sudo ./wsl_start_sender.sh 172.26.43.251
```

### Вариант 2: Через UDP Relay (если прямое не работает)

**На устройстве B (Windows):**
1. Отредактируйте `udp_relay.py` - укажите WSL IP
2. Запустите: `python udp_relay.py`

**На устройстве A (WSL):**
```bash
# Подключайтесь к Windows IP устройства B
sudo ./wsl_start_sender.sh 192.168.100.101
```

---

## 🐛 Отладка

### Проверка UDP связности:

```bash
# На устройстве B
sudo lsof -i UDP:12346

# На устройстве A
echo "test" | nc -u <IP_B> 12346
```

### Мониторинг трафика:

```bash
sudo tcpdump -i any -n 'udp port 12346'
```

### Логи программ:

Программы выводят подробные логи:
- `✅ UDP сокет подключён...` - успешное подключение
- `📥 Публичный ключ получен от X.X.X.X:XXXXX` - показывает адрес отправителя
- `📤/📥 Отправлен/Принят кадр` - работает передача данных

---

## 🔄 Переключение веток

### Вернуться на основную ветку:

```bash
git checkout main
```

### Переключиться на WSL ветку:

```bash
git checkout wsl-optimization
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте `WSL_SETUP_GUIDE.md` - раздел "Решение проблем"
2. Убедитесь что используете WSL 2: `wsl --list --verbose`
3. Проверьте брандмауэр Windows
4. Используйте `tcpdump` для диагностики

---

## ✅ Тестирование пройдено на:

- Windows 10 (21H2) + WSL 2
- Windows 11 (22H2) + WSL 2  
- Ubuntu 20.04/22.04 в WSL
- Сеть: Wi-Fi и Ethernet

---

**Версия:** 2.0-wsl
**Дата:** Декабрь 2024

