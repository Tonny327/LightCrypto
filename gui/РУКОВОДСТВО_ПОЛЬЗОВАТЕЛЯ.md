# 📘 Руководство пользователя LightCrypto GUI

> **Полное руководство по использованию исполняемого файла LightCrypto GUI**

---

## 📑 Оглавление

1. [Введение](#-введение)
2. [Требования системы](#-требования-системы)
3. [Установка и запуск](#-установка-и-запуск)
4. [Перенос на другой компьютер](#-перенос-на-другой-компьютер)
5. [Быстрый старт](#-быстрый-старт)
6. [Основные функции](#-основные-функции)
7. [Режимы шифрования](#-режимы-шифрования)
8. [Работа с приложением](#-работа-с-приложением)
9. [Устранение проблем](#-устранение-проблем)
10. [FAQ](#-faq)
11. [Техническая поддержка](#-техническая-поддержка)

---

## 📖 Введение

### Что такое LightCrypto GUI?

**LightCrypto GUI** — это графическое приложение для защищенной передачи данных между двумя компьютерами через зашифрованное соединение. Приложение создает виртуальные сетевые интерфейсы (TAP), через которые весь трафик автоматически шифруется.

### Основные возможности

- ✅ **Два метода шифрования**: LibSodium (промышленный стандарт) и Custom Codec (экспериментальный)
- ✅ **Простой интерфейс**: интуитивно понятный графический интерфейс
- ✅ **Автоматическое шифрование**: весь трафик шифруется прозрачно
- ✅ **Встроенный терминал**: полный контроль над процессом
- ✅ **Тестовые утилиты**: ping, iperf и другие инструменты для проверки связи
- ✅ **Режим сообщений**: обмен текстовыми сообщениями
- ✅ **Режим файлов**: безопасная передача файлов

### Для кого это приложение?

- Пользователи, которым нужна защищенная связь между двумя компьютерами
- Те, кто хочет зашифровать весь сетевой трафик между устройствами
- Исследователи, изучающие криптографию
- Пользователи, которым нужен простой способ настройки VPN-подобного соединения

---

## 💻 Требования системы

### Минимальные требования

- **ОС**: Linux (x86_64)
- **Архитектура**: 64-битная
- **Права доступа**: sudo (для создания TAP интерфейсов)
- **Сеть**: два компьютера в одной сети или с доступом друг к другу

### Рекомендуемые требования

- **ОС**: Ubuntu 20.04+, Debian 11+, или другой современный Linux дистрибутив
- **RAM**: минимум 512 МБ свободной памяти
- **Дисковое пространство**: ~20 МБ для исполняемого файла
- **Сеть**: стабильное соединение между компьютерами

### Что НЕ требуется

- ❌ Установка Python
- ❌ Установка дополнительных библиотек
- ❌ Компиляция исходного кода
- ❌ Сложная настройка системы

---

## 📥 Установка и запуск

### Шаг 1: Получение исполняемого файла

Исполняемый файл `lightcrypto` должен быть предоставлен вам разработчиком или собран из исходников.

**Размер файла**: ~14 МБ

**Важно**: Исполняемый файл является полностью самодостаточным. Он включает все необходимые компоненты:
- Исполняемые файлы `tap_encrypt` и `tap_decrypt`
- CSV файлы с ключами шифрования (для Custom Codec)
- Скрипты настройки TAP интерфейсов
- Все библиотеки Python и зависимости

При переносе на другой компьютер вам нужно скопировать **только один файл** — `lightcrypto`.

### Шаг 2: Установка прав доступа

```bash
chmod +x lightcrypto
```

### Шаг 3: Настройка sudo (однократно)

Для работы приложения необходим sudo доступ без пароля для создания TAP интерфейсов:

```bash
# Создайте файл для правил sudo
sudo visudo -f /etc/sudoers.d/lightcrypto

# Добавьте следующую строку (замените USERNAME на ваше имя пользователя):
USERNAME ALL=(ALL) NOPASSWD: /sbin/ip, /sbin/ifconfig, /usr/bin/tunctl, /sbin/modprobe
```

**Или** используйте скрипт настройки (если он предоставлен):
```bash
sudo ./setup_sudo.sh
```

### Шаг 4: Запуск приложения

**Способ 1: Из текущей директории**
```bash
./lightcrypto
```

**Способ 2: Из любой директории (полный путь)**
```bash
/path/to/lightcrypto
```

**Способ 3: Добавить в PATH (для удобства)**
```bash
# Временно
export PATH=$PATH:/path/to/directory/with/lightcrypto
lightcrypto

# Постоянно (добавить в ~/.bashrc)
echo 'export PATH=$PATH:/path/to/directory/with/lightcrypto' >> ~/.bashrc
source ~/.bashrc
```

### Первый запуск

При первом запуске приложение:
1. Проверит системные требования
2. Проверит наличие необходимых исполняемых файлов (`tap_encrypt`, `tap_decrypt`)
3. Откроет графический интерфейс

**Важно**: Исполняемые файлы `tap_encrypt` и `tap_decrypt` **уже включены** в исполняемый файл `lightcrypto`. При переносе на другой компьютер вам нужно скопировать **только один файл** — сам `lightcrypto`. Все необходимые компоненты (включая `tap_encrypt`, `tap_decrypt`, CSV файлы и скрипты) уже находятся внутри исполняемого файла.

---

## 📦 Перенос на другой компьютер

### Что нужно скопировать?

**Только один файл**: `lightcrypto`

Исполняемый файл является полностью самодостаточным и включает:
- ✅ Исполняемые файлы `tap_encrypt` и `tap_decrypt`
- ✅ Все CSV файлы с ключами шифрования (15 файлов)
- ✅ Скрипты настройки TAP интерфейсов (`setup_tap_*.sh`)
- ✅ Все библиотеки Python и зависимости
- ✅ Все модули приложения

### Инструкция по переносу

1. **Скопируйте файл на целевой компьютер:**
   ```bash
   # Через SCP
   scp lightcrypto user@remote:/path/to/destination/
   
   # Или через USB/другой носитель
   # Просто скопируйте файл lightcrypto
   ```

2. **На целевом компьютере установите права:**
   ```bash
   chmod +x lightcrypto
   ```

3. **Настройте sudo (если еще не настроено):**
   ```bash
   sudo visudo -f /etc/sudoers.d/lightcrypto
   # Добавьте: USERNAME ALL=(ALL) NOPASSWD: /sbin/ip, /sbin/ifconfig, /usr/bin/tunctl, /sbin/modprobe
   ```

4. **Запустите приложение:**
   ```bash
   ./lightcrypto
   ```

**Важно**: Не нужно копировать никакие дополнительные файлы или директории. Все необходимое уже включено в исполняемый файл.

### Проверка после переноса

После переноса на другой компьютер приложение автоматически:
1. Распакует все необходимые файлы во временную директорию
2. Проверит наличие всех компонентов
3. Запустится как обычно

Если вы видите ошибку "Исполняемые файлы не найдены", это означает, что файл был собран неправильно. Обратитесь к разработчику для получения правильно собранной версии.

---

## 🚀 Быстрый старт

### Сценарий: Защищенное соединение между двумя компьютерами

#### Подготовка (на обоих компьютерах)

1. Скопируйте исполняемый файл `lightcrypto` на оба компьютера
2. Убедитесь, что на обоих компьютерах настроен sudo без пароля
3. Убедитесь, что исполняемые файлы `tap_encrypt` и `tap_decrypt` доступны

#### Компьютер A (Отправитель)

1. Запустите приложение: `./lightcrypto`
2. Выберите: **🔒 LibSodium** → **📤 Отправитель**
3. Нажмите кнопку **"🔧 Создать TAP-A"**
4. Введите IP-адрес компьютера B (например: `192.168.1.100`)
5. Нажмите **"▶️ ЗАПУСТИТЬ ШИФРОВАНИЕ"**

#### Компьютер B (Получатель)

1. Запустите приложение: `./lightcrypto`
2. Выберите: **🔒 LibSodium** → **📥 Получатель**
3. Нажмите кнопку **"🔧 Создать TAP-B"**
4. Нажмите **"▶️ ЗАПУСТИТЬ ШИФРОВАНИЕ"**

#### Проверка соединения

На компьютере A выполните:
```bash
ping 10.0.0.2
```

Если ping работает, соединение установлено и весь трафик шифруется!

---

## 🎯 Основные функции

### Интерфейс приложения

Приложение состоит из трех основных окон:

1. **Стартовое окно** — выбор типа шифрования (LibSodium или Custom Codec)
2. **Окно выбора роли** — выбор роли (Отправитель или Получатель)
3. **Рабочее окно** — основной интерфейс с настройками и управлением

### Основные элементы интерфейса

- **Кнопка создания TAP интерфейса** — создает виртуальный сетевой интерфейс
- **Поля ввода IP и порта** — настройка сетевых параметров
- **Кнопка запуска/остановки** — управление процессом шифрования
- **Встроенный терминал** — отображение логов и выполнение команд
- **Панель статуса** — информация о текущем состоянии

### Автосохранение настроек

Все введенные параметры автоматически сохраняются и восстанавливаются при следующем запуске.

---

## 🔐 Режимы шифрования

### 🔒 LibSodium (рекомендуется)

**Характеристики:**
- **Алгоритм**: ChaCha20-Poly1305 AEAD
- **Обмен ключами**: X25519 (ECDH)
- **Аутентификация**: Poly1305 MAC
- **Использование**: Промышленное (используется в Signal, WhatsApp)
- **Скорость**: Очень высокая
- **Надежность**: Проверенная временем

**Когда использовать:**
- Для реальных задач, требующих надежного шифрования
- Когда нужна максимальная производительность
- Для производственного использования

### ⚡ Custom Digital Codec (экспериментальный)

**Характеристики:**
- **Алгоритм**: Цифровое кодирование с нелинейными функциями
- **Ключ**: CSV файл с коэффициентами
- **Аутентификация**: SHA-256 хеш
- **Использование**: Исследовательское
- **Настройка**: Требует выбора CSV файла и параметров

**Когда использовать:**
- Для экспериментов и исследований
- Для изучения криптографии
- Когда нужна настройка параметров шифрования

**Требования:**
- CSV файлы с ключами должны находиться в директории `CipherKeys/`
- Необходимо правильно настроить параметры M, Q, h1, h2

---

## 💼 Работа с приложением

### Создание TAP интерфейса

1. Нажмите кнопку **"🔧 Создать TAP-A"** (для отправителя) или **"🔧 Создать TAP-B"** (для получателя)
2. Дождитесь сообщения об успешном создании
3. Проверьте статус в панели статуса

**Примечание**: Для создания TAP интерфейса требуются права sudo.

### Настройка сетевых параметров

**Для отправителя:**
- **IP получателя**: Введите IP-адрес компьютера-получателя в вашей сети
- **Порт**: По умолчанию 12345 (можно изменить)

**Для получателя:**
- **IP для прослушивания**: Оставьте `0.0.0.0` для прослушивания на всех интерфейсах
- **Порт**: Должен совпадать с портом отправителя

### Запуск шифрования

1. Убедитесь, что TAP интерфейс создан на обоих компьютерах
2. Нажмите кнопку **"▶️ ЗАПУСТИТЬ ШИФРОВАНИЕ"** сначала на получателе, затем на отправителе
3. Проверьте статус в терминале — должны появиться сообщения о подключении

### Остановка шифрования

Нажмите кнопку **"⏹️ ОСТАНОВИТЬ"** на любом из компьютеров.

### Использование встроенного терминала

Встроенный терминал позволяет:
- Видеть логи работы приложения
- Выполнять команды для проверки соединения
- Использовать утилиты: ping, iperf, hping3

**Примеры команд:**
```bash
# Проверка связи
ping 10.0.0.2

# Проверка скорости
iperf3 -c 10.0.0.2

# Просмотр сетевых интерфейсов
ip addr show
```

### Режим сообщений

1. Включите переключатель **"Режим сообщений"**
2. Введите текст в поле ввода
3. Нажмите кнопку отправки
4. Сообщение будет зашифровано и отправлено получателю

### Режим передачи файлов

1. Включите переключатель **"Режим файлов"**
2. Выберите файл для отправки (кнопка **"Выбрать файл"**)
3. Нажмите кнопку отправки
4. Файл будет зашифрован, разбит на фрагменты и отправлен
5. На получателе файл будет автоматически собран и сохранен

---

## 🔧 Устранение проблем

### Проблема: "команда не найдена" при запуске

**Решение:**
Используйте `./lightcrypto` (с точкой и слэшем) вместо просто `lightcrypto`:
```bash
./lightcrypto
```

Или используйте полный путь к файлу.

### Проблема: "Permission denied"

**Решение:**
Установите права на выполнение:
```bash
chmod +x lightcrypto
```

### Проблема: "Не удалось создать TAP интерфейс"

**Возможные причины:**
1. Нет прав sudo
2. Модуль ядра `tun` не загружен
3. TAP интерфейс уже существует

**Решения:**
```bash
# Проверка прав sudo
sudo -n true

# Загрузка модуля tun
sudo modprobe tun

# Проверка существующих TAP интерфейсов
ip link show | grep tap

# Удаление существующего интерфейса (если нужно)
sudo ip link delete tap0
```

### Проблема: "Исполняемые файлы не найдены"

**Причина:**
Исполняемые файлы `tap_encrypt` и `tap_decrypt` должны быть включены в исполняемый файл при сборке. Если вы видите эту ошибку, возможно:
1. Исполняемый файл был собран неправильно
2. Файлы не были включены в сборку

**Решение:**
1. **Если вы получили готовый исполняемый файл:**
   - Обратитесь к разработчику — файл должен быть пересобран с включением всех компонентов
   
2. **Если вы собираете файл самостоятельно:**
   - Убедитесь, что файлы `tap_encrypt` и `tap_decrypt` существуют в директории `build/` перед сборкой
   - Запустите скрипт сборки: `./build_executable.sh`
   - Скрипт автоматически включит все необходимые файлы

**Примечание:** Правильно собранный исполняемый файл является самодостаточным и не требует дополнительных файлов при переносе на другой компьютер.

### Проблема: "Не удается подключиться"

**Проверьте:**
1. Правильность IP-адреса получателя
2. Совпадение портов на обоих компьютерах
3. Наличие файрвола, блокирующего соединение
4. Запущено ли приложение на обоих компьютерах

**Диагностика:**
```bash
# Проверка доступности порта
nc -zv IP_АДРЕС ПОРТ

# Проверка файрвола
sudo ufw status

# Просмотр активных соединений
netstat -tuln | grep ПОРТ
```

### Проблема: "CSV файлы не найдены" (для Custom Codec)

**Решение:**
Убедитесь, что CSV файлы находятся в директории `CipherKeys/` относительно исполняемого файла:
```
/path/to/lightcrypto
/path/to/CipherKeys/
  ├── Q=2.csv
  ├── Q=2_2.csv
  └── ...
```

### Проблема: Приложение не запускается

**Проверьте:**
1. Совместимость архитектуры (должна быть x86_64)
2. Наличие необходимых системных библиотек
3. Логи ошибок в терминале

**Запуск с выводом ошибок:**
```bash
./lightcrypto 2>&1 | tee error.log
```

---

## ❓ FAQ

### В: Нужно ли устанавливать Python?

**О:** Нет, исполняемый файл самодостаточен и включает все необходимые компоненты.

### В: Можно ли использовать на Windows или macOS?

**О:** Нет, исполняемый файл создан только для Linux (x86_64). Для других платформ нужна пересборка.

### В: Безопасно ли это приложение?

**О:** LibSodium режим использует проверенные криптографические алгоритмы. Custom Codec является экспериментальным и предназначен для исследований.

### В: Можно ли использовать для передачи больших файлов?

**О:** Да, приложение поддерживает передачу файлов любого размера через режим файлов.

### В: Требуется ли интернет для работы?

**О:** Нет, приложение работает в локальной сети. Интернет не требуется.

### В: Можно ли использовать на разных дистрибутивах Linux?

**О:** Да, если это x86_64 Linux. Могут потребоваться системные библиотеки, но обычно они уже установлены.

### В: Нужно ли копировать дополнительные файлы при переносе на другой компьютер?

**О:** Нет! Исполняемый файл `lightcrypto` является полностью самодостаточным. Он включает:
- Исполняемые файлы `tap_encrypt` и `tap_decrypt`
- Все CSV файлы с ключами шифрования
- Скрипты настройки TAP интерфейсов
- Все необходимые библиотеки Python

Просто скопируйте один файл `lightcrypto` на другой компьютер и запустите его.

### В: Как удалить приложение?

**О:** Просто удалите исполняемый файл:
```bash
rm lightcrypto
```

### В: Сохраняются ли настройки между запусками?

**О:** Да, все настройки автоматически сохраняются в конфигурационный файл.

### В: Можно ли использовать несколько соединений одновременно?

**О:** Теоретически да, но рекомендуется использовать одно соединение за раз для избежания конфликтов.

---

## 📞 Техническая поддержка

### Получение помощи

Если у вас возникли проблемы:

1. **Проверьте раздел "Устранение проблем"** в этом руководстве
2. **Проверьте логи** в терминале приложения
3. **Обратитесь к разработчику** с описанием проблемы

### Информация для отчета об ошибке

При обращении за помощью предоставьте:
- Версию операционной системы
- Версию исполняемого файла (если известна)
- Описание проблемы
- Сообщения об ошибках из терминала
- Шаги для воспроизведения проблемы

### Дополнительная документация

Для более подробной информации см.:
- `README_GUI.md` — полное техническое руководство
- Исходный код проекта (если доступен)

---

## 📝 Заключение

LightCrypto GUI — это мощный инструмент для защищенной передачи данных между двумя компьютерами. Следуя этому руководству, вы сможете быстро настроить и использовать приложение.

**Помните:**
- Всегда проверяйте соединение перед передачей важных данных
- Используйте LibSodium режим для реальных задач
- Custom Codec предназначен для экспериментов
- Регулярно обновляйте приложение для получения исправлений безопасности

**Удачного использования!** 🔐

---

*Версия документа: 1.0*  
*Дата обновления: 2025*

