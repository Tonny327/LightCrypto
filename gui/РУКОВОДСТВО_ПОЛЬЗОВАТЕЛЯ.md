# 📘 Руководство пользователя LightCrypto GUI — режим **Custom Codec**

> Этот документ посвящён только пользовательскому режиму **Custom Digital Codec**. Здесь описаны требования, порядок действий для двух компьютеров, назначение каждого элемента интерфейса, объяснение параметров `M`, `Q`, `fun`, `h1`, `h2`, а также использование отладки, статистики и искусственных ошибок. Скриншоты можно вставить в отмеченные места.

---

## 1. Что такое Custom Codec

- **Суть**: собственный помехоустойчивый кодек, работающий с парами состояний `(h, v)` и коэффициентами из CSV-файла. Он кодирует произвольные данные в последовательность блоков и может исправлять одиночные ошибки.
- **Когда выбирать**: если нужно протестировать алгоритм цифрового кодирования, исследовать поведение при помехах или использовать заранее подготовленный CSV-набор коэффициентов.
- **Плюсы**: гибко настраивается, даёт подробную статистику и отладочный вывод.
- **Минусы**: требует тщательного согласования параметров, чувствителен к рассинхронизации состояний.

---

## 2. Подготовка окружения

| Шаг | Команда/действие |
|-----|------------------|
| 1. Клонировать/обновить репозиторий | `git clone …` |
| 2. Собрать нативные бинарники | `./rebuild.sh` (создаёт `build/tap_encrypt` и `build/tap_decrypt`) |
| 3. Настроить sudo без пароля | `sudo ./setup_sudo.sh` (разрешает запуск скриптов управления TAP) |
| 4. Запустить GUI | `cd gui && python3 main.py` |

> _Скриншот 1: стартовое окно с выбором «⚡ Custom Codec»._

---

## 3. Общая последовательность действий (две машины)

1. **Запуск GUI** на обеих сторонах → выбрать `Custom Codec`.
2. **Выбор роли**:
   - Отправитель (`📤 Encrypt`) запускает `tap_encrypt`.
   - Получатель (`📥 Decrypt`) запускает `tap_decrypt`.
3. **Настройка параметров кодека**: обе стороны должны выставить **одинаковый** набор `CSV, M, Q, fun, h1, h2`.
4. **Создание TAP-интерфейсов**:
   - Отправитель → `🔧 Создать TAP-A` (tap0, 10.0.0.1/24).
   - Получатель → `🔧 Создать TAP-B` (tap1, 10.0.0.2/24).
5. **Настройка IP/порта**:
   - Получатель указывает порт, который будет слушать, и нажимает `▶️`.
   - Отправитель вводит IP получателя и тот же порт, затем нажимает `▶️`.
6. **Проверка**:
   - Режим Ethernet — `ping 10.0.0.2` со стороны отправителя.
   - Режим сообщений — просто ввести текст в терминале отправителя.
7. **Завершение**:
   - Нажать `⏹` → `🧹 Очистить TAP` → закрыть GUI (`Ctrl+C`).

---

## 4. Интерфейс Custom Codec окна

> _Скриншот 2: рабочее окно отправителя Custom Codec._

Основные блоки:

1. **Панель CSV и параметров алгоритма** — сердцевина режима:
   - Выбор файла CSV (`CipherKeys/*.csv`).
   - Автоанализ (строки, столбцы, рекомендуемые M/Q, допустимые `funType`).
   - Спинбоксы и комбо-боксы для `M`, `Q`, `fun`, `h1`, `h2`.
   - Секция «Тестирование и отладка» (флаги debug/stats/error injection).
2. **Управление TAP**:
   - `🔧 Создать TAP-A/B`
   - `🧹 Очистить TAP`
   - Статус с подсветкой (`🟢 tap0 активен …`).
3. **Сетевые параметры**:
   - IP и порт.
   - Галочка «Режим сообщений (--msg)».
   - (Отправитель) поле для выбора файла при передаче через кодек (режим `--file`).
4. **Кнопка запуска** `▶️ ЗАПУСТИТЬ КОДЕК`.
5. **Терминал**:
   - Показывает ход исполнения, сообщения об ошибках, статистику и отладочный вывод.
   - Поддерживает ввод в режиме сообщений.
6. **Служебные кнопки** (опционально):
   - Генерация тестового трафика (ping/iperf/hping3).
   - Управление профилями параметров (`💾 Сохранить`, `📂 Загрузить`).

---

## 5. Подробно о параметрах Custom Codec

| Параметр | Где задаётся | Что означает | Рекомендации |
|----------|--------------|--------------|--------------|
| `CSV файл` | Выпадающий список + «Обзор» | Таблица коэффициентов `(a,b,c,q)` для функций DigitalCodingFun. | Используйте файлы из `CipherKeys/`. После выбора посмотрите блок «Авто-определено». |
| `M` (bitsM) | Спинбокс | Разрядность регистров состояния. Определяет диапазон значений `h` и `v`. | Лучше следовать рекомендованному M из CSV. Увеличение повышает надёжность, но и размер кадров. |
| `Q` (bitsQ) | Спинбокс, опция «Авто из CSV» | Количество информационных бит на символ. Таблица должна содержать `2^Q` строк. | Всегда согласовывайте с CSV. Сняв галочку «Авто», можно задать вручную, но следите за валидностью. |
| `funType` | Комбо-бокс | Номер набора нелинейных функций (1…5). | В списке помечено, какие типы доступны для выбранного CSV. |
| `h1`, `h2` | Спинбоксы / `🎲 Случайно` | Начальные состояния генератора. | Должны совпадать на обеих сторонах. Для тестов можно случайно генерировать и обмениваться значениями. |
| `Режим отладки` | Чекбокс | Подробный лог каждого символа (какая функция сработала, где пропуск). | Включайте на этапе отладки. Вывод очень подробный. |
| `Сбор статистики` | Чекбокс | Агрегированные метрики (коллизии, пропуски, прямые передачи). | Полезно при тестах. GUI выводит статистику после первого кадра и каждые 10 кадров. |
| `Искусственное внесение ошибок` | Чекбокс + спинбокс `%` | Инвертирует случайные биты после кодирования (симуляция каналов с шумом). | Ставьте малые значения (0.5–5 %) для мягких тестов, большие — для стресс-тестов. |
| `Профили` | Кнопки `💾 / 📂 / 🔄` | Сохранение/загрузка набора параметров в JSON. | Храните типовые конфигурации. Файлы лежат в `gui/profiles/custom_codec/`. |

---

## 6. Пошаговый сценарий для Custom Codec

### 6.1. Отправитель

1. Выбрать CSV и убедиться, что «Авто-определено» зелёным цветом.
2. Проверить/подкорректировать M/Q/fun/h1/h2.
3. При необходимости включить `Режим отладки`, `Сбор статистики`, `Искусственное внесение ошибок`.
4. Нажать `🔧 Создать TAP-A` и дождаться статуса `🟢`.
5. В сетевой панели указать IP получателя и порт (например, `192.168.1.10:5555`).
6. Нажать `▶️ ЗАПУСТИТЬ КОДЕК`.
7. Наблюдать терминал:
   - вывод о синхронизации состояний,
   - отчёты о переданных кадрах/сообщениях,
   - при включённой статистике — блоки `📊 …`.
8. При режиме сообщений вводить текст прямо в терминал GUI.

### 6.2. Получатель

1. Выбрать тот же CSV и параметры.
2. Нажать `🔧 Создать TAP-B`.
3. Указать порт (IP можно оставить `0.0.0.0`, чтобы слушать все интерфейсы).
4. Нажать `▶️ ЗАПУСТИТЬ КОДЕК` — терминал покажет «Ожидаем пакеты…».
5. Приёмник автоматически:
   - принимает кадры/сообщения,
   - отображает содержимое (либо пишет файл в режиме передачи файлов),
   - выводит статистику/отладку при включенных флагах.

### 6.3. Очерёдность запуска

1. **Всегда запускайте получателя первым**, чтобы порт уже слушал.
2. Затем запускайте отправителя.
3. После завершения передачи сначала остановите отправителя, потом получателя.

---

## 7. Режимы передачи

| Режим | Как включить | Что делает |
|-------|--------------|------------|
| Ethernet (по умолчанию) | `Режим сообщений` **выключен**, `--file` не задан | Любые данные, читаемые из tap0, кодируются и отправляются как кадры. Подходит для туннелирования IP. |
| Сообщения (`--msg`) | Галочка «Режим сообщений» в сетевой панели | GUI показывает поле ввода, текст отправляется как отдельные сообщения. Терминал получателя выводит строки. |
| Передача файлов | Кнопка «Передать файл» (отправитель) → выбрать путь | Файл разбивается на чанки, каждый кодируется кодеком и подтверждается через ACK. Получатель сохраняет в `./received_file` (или путь из `--output`). |

> _Скриншот 3: пример терминала при передаче файла с включённой статистикой._

---

## 8. Диагностика, статистика и отладка

### 8.1. Режим отладки

- Включается чекбоксом «Режим отладки».
- Показывает:
  - значения `h`, `v`, `x`, `y` перед декодированием,
  - результаты поиска совпадений,
  - сообщения о пропуске символов.
- Подходящ на ранних стадиях, но генерирует большой объём логов.

### 8.2. Сбор статистики

- Выводит агрегаты (количество символов, коллизий, пропусков).
- Появляется:
  - сразу после первого кадра/сообщения,
  - каждые 10 кадров,
  - при завершении программы.
- Пример:
  ```
  📊 Статистика кодека (отправитель, каждые 10 кадров)
     🔢 Закодировано символов: 320
     ⚠️  Коллизий обнаружено: 1
     🎲 Случайных подстановок: 1
     📡 Прямых передач Info: 0
     ✅ Декодировано символов: 318
     ℹ️  Декодировано через Info: 0
     ⛔ Пропусков при декодировании: 2
  ```

### 8.3. Искусственные ошибки

- Включаются чекбоксом «Искусственное внесение ошибок».
- В GUI задаётся вероятность (%) — это вероятность инверсии одного случайного бита в каждом кодовом блоке.
- Используется только на отправителе (получатель исправляет или сигнализирует о пропусках).
- В отчётности видно, насколько алгоритм устойчив при разных процентах ошибок.

### 8.4. Обработка пропусков

- Если декодер не находит совпадение, он выводит предупреждение и, при необходимости, запрашивает повторную синхронизацию.
- На практике:
  - Если пропусков много → проверьте параметр `h1/h2` и синхронизацию состояний (в терминале видно `🔄 Синхронизация состояний`).
  - Если пропуски появляются только при включённых ошибках — уменьшите процент или активируйте сбор статистики для анализа.

---

## 9. Профили и управление конфигурацией

- Кнопка `💾 Сохранить` — записывает текущий набор параметров (CSV, M, Q, fun, h1, h2, флаги отладки) в JSON-файл.
- `📂 Загрузить` — выбирает существующий профиль.
- `🔄 Сброс` — возвращает значения по умолчанию (M=8, Q=6, fun=1, h1=7, h2=23, флаги сняты).
- Все профили хранятся в `gui/profiles/custom_codec/`.
- Полезно держать отдельные профили для разных CSV, наборов M/Q и сценариев тестирования.

---

## 10. Типовые проблемы и решения

| Симптом | Возможная причина | Что делать |
|---------|-------------------|------------|
| Нет статистики | Забыт чекбокс «Сбор статистики» или передано < 1 кадра | Включите флаг; теперь статистика выводится после первого кадра и каждые 10 кадров. |
| Сообщения «Пропущен символ при декодировании» | Несовпадение параметров, ошибки канала или искусственные помехи | Проверьте CSV, M/Q/fun, h1/h2. Если включено внесение ошибок — уменьшите процент. |
| Ошибка создания TAP | tap0/tap1 уже существует или не хватает прав | Нажмите `🧹 Очистить TAP` или выполните `sudo ip link delete tap0`. Убедитесь, что `setup_sudo.sh` был запущен. |
| Терминал пуст после запуска | Получатель не запущен или неверный IP/порт | Всегда стартуйте получателя первым; проверьте IP-адрес в сетевой панели отправителя. |
| CSV не подхватывается | Файл не в каталоге `CipherKeys/` или повреждён | Переместите CSV в `CipherKeys/`, убедитесь, что строк = `2^Q`, столбцов 3 или 4. |

---

## 11. Практические советы

1. **Согласование параметров**: перед запуском обменяйтесь скриншотом панели или используйте общий профиль.
2. **M/Q**: начинайте с `M=8`/`Q=6` и CSV из поставки (например, `Q=2_6.csv`), затем экспериментируйте.
3. **Отладка + статистика**: включайте вместе, когда нужно понять поведение кода при реальных ошибках.
4. **Искусственные ошибки**: начинайте с 1 %, увеличивайте до 5–10 % для стресс-теста.
5. **Режим сообщений**: удобен для быстрой проверки связи; если сообщения доходят, значит параметры совпадают.
6. **Передача файлов**: используйте, когда нужно проверить работу ACK/пересинхронизации. Смотрите статистику — она покажет, сколько символов было исправлено.

---

## 12. Структура каталогов (для Custom Codec)

```
LightCrypto/
├── CipherKeys/                 # CSV-файлы коэффициентов
├── gui/
│   ├── custom/codec_panel.py   # код GUI панели Custom Codec
│   ├── profiles/custom_codec/  # сохранённые профили
│   └── РУКОВОДСТВО_ПОЛЬЗОВАТЕЛЯ.md  # текущий документ
└── src/
    ├── digital_codec.*         # ядро кодека
    ├── tap_encrypt.cpp         # отправитель
    └── tap_decrypt.cpp         # получатель
```

---

## 13. Где добавлять скриншоты

| Раздел | Подпись для изображения |
|--------|-------------------------|
| §2 | `![Стартовое окно Custom Codec](images/codec_start.png)` |
| §4 | `![Рабочее окно Custom Codec](images/codec_main.png)` |
| §5 | `![Панель параметров](images/codec_params.png)` |
| §7 | `![Пример терминала](images/codec_terminal.png)` |

Замените `images/...` на фактические пути к изображениям.

---

### Готово

С помощью этого руководства вы можете полностью настроить и использовать режим **Custom Codec** в LightCrypto GUI: от выбора CSV и параметров M/Q/fun до мониторинга статистики и имитации ошибок. Используйте профили, отладочный вывод и встроенные инструменты, чтобы адаптировать кодек под свои задачи и проводить эксперименты с помехоустойчивостью. Удачных испытаний!


