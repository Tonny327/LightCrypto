# 🖥️ LightCrypto GUI - Инструкция по использованию

Новая версия GUI со встроенным терминалом для удобного управления процессами.

## 🚀 Быстрый старт

```bash
# 1. Настройка sudo доступа (если еще не сделано)
bash setup_sudo.sh

# 2. Запуск GUI приложений
python3 gui/decrypt_gui.py &    # В фоне
python3 gui/encrypt_gui.py      # Основное окно
```

## 🖥️ Интерфейс

### Элементы управления:
- **🟢 "Запустить задачу"** → **🔴 "Остановить задачу"**
- **🧹 "Очистить терминал"** - очистка вывода
- **⚙️ Поля настроек** - IP, порт, режим сообщений

### Встроенный терминал:
- **Черный фон, зеленый текст** (как настоящий Linux терминал)
- **Реальный вывод** программ tap_encrypt/tap_decrypt
- **Автопрокрутка** к новым сообщениям
- **Размер**: 600x700 для удобного просмотра

## 🔧 Управление процессами

### Запуск:
1. Настройте IP и порт (или оставьте по умолчанию)
2. Включите режим сообщений если нужно
3. Нажмите **"Запустить задачу"**
4. Наблюдайте вывод в терминале

### Остановка:
1. Нажмите **"Остановить задачу"** (красная кнопка)
2. Программа корректно завершит процесс:
   - Сначала SIGTERM (мягкое завершение)
   - Если не помогает - SIGKILL (принудительное)
   - Завершает всю группу процессов

### Очистка:
- Нажмите **"Очистить терминал"** в любой момент
- Терминал очистится и покажет сообщение "🧹 Терминал очищен"

## 📺 Что вы увидите

### При запуске gui/encrypt_gui.py:
```
🔍 Проверка sudo доступа...
✅ Sudo доступ настроен корректно
🌐 Доступные неймспейсы:
   ns1 (id: 0)
   ns2 (id: 1)

============================================================
🚀 Запуск команды: sudo ip netns exec ns1 ./build/tap_encrypt 192.168.1.2 12345
============================================================
🔧 Процесс запущен в фоновом режиме
🌐 Используем IP: 192.168.1.2, порт: 12345
✅ IP-адрес 192.168.1.2 доступен, начинаем работу...
📡 tap0 открыт для чтения Ethernet-кадров
📤 Публичный ключ отправлен получателю
📥 Публичный ключ получен от получателя
📤 Отправлен зашифрованный кадр (64 байт)
📤 Отправлен зашифрованный кадр (96 байт)
```

### При остановке:
```
🛑 Остановка процесса...
✅ Процесс завершен корректно
🏁 Готов к новому запуску
```

## 🆚 Отличия от старой версии

| Старая версия | Новая версия |
|---------------|--------------|
| Белый фон консоли | Черный терминал с зеленым текстом |
| PTY эмуляция | Простой subprocess |
| Проблемы с остановкой | Надежная остановка через сигналы |
| Нет очистки | Кнопка "Очистить терминал" |
| 500x600 | 600x700 |

## 💾 Backup файлы

Старые версии сохранены как:
- `encrypt_gui_pty_backup.py`
- `decrypt_gui_pty_backup.py`

## 🔧 Устранение проблем

### Процесс не останавливается:
- GUI автоматически использует SIGTERM → SIGKILL
- Завершает группу процессов целиком
- Если проблема повторяется - перезапустите GUI

### Нет вывода в терминале:
- Проверьте sudo доступ: `bash setup_sudo.sh`
- Убедитесь что программы собраны: `cd build && make`
- Проверьте неймспейсы: `sudo ip netns list`

### GUI не запускается:
```bash
python3 -c "import tkinter; print('tkinter OK')"
python3 gui/encrypt_gui.py  # Посмотрите ошибки
```

## 🎯 Готово!

Новый GUI с встроенным терминалом обеспечивает:
- ✅ Полный контроль над процессами
- ✅ Реальный вывод программ
- ✅ Удобную очистку терминала
- ✅ Надежную остановку процессов

Наслаждайтесь улучшенным интерфейсом! 🚀
