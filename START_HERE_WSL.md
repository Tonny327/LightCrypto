# 🚀 Начните здесь - LightCrypto WSL версия

> **Вы находитесь в ветке WSL-optimization**

---

## ✅ Что уже сделано для вас:

1. ✅ **tap_encrypt.cpp** оптимизирован для WSL (добавлен `connect()`)
2. ✅ **tap_decrypt.cpp** улучшен для отладки WSL (расширенное логирование)
3. ✅ Созданы **автоматические скрипты** запуска
4. ✅ Подготовлен **UDP relay** для сложных сетевых конфигураций
5. ✅ Написана **полная документация**

---

## 🎯 Что нужно сделать:

### Шаг 1: Скомпилируйте проект (на обоих устройствах)

```bash
cd ~/LightCrypto
mkdir -p build

g++ -o build/tap_encrypt src/tap_encrypt.cpp src/digital_codec.cpp -lsodium -pthread
g++ -o build/tap_decrypt src/tap_decrypt.cpp src/digital_codec.cpp -lsodium -pthread

chmod +x wsl_start_receiver.sh wsl_start_sender.sh
```

### Шаг 2: Прочитайте инструкцию

**Выберите один из файлов:**

- 📄 **`QUICKSTART_WSL.txt`** - Если хотите быстро начать (5 минут чтения)
- 📖 **`WSL_SETUP_GUIDE.md`** - Если хотите все подробности (15 минут чтения)

### Шаг 3: Запустите на двух устройствах

**На устройстве B (приёмник):**
```bash
sudo ./wsl_start_receiver.sh
```

**На устройстве A (отправитель):**
```bash
# Узнайте IP устройства B: hostname -I (на устройстве B)
sudo ./wsl_start_sender.sh <IP_устройства_B>
```

### Шаг 4: Тестируйте

```bash
ping 10.0.0.2
```

---

## 📚 Полный список файлов документации:

| Файл | Описание | Когда читать |
|------|----------|--------------|
| **START_HERE_WSL.md** | Этот файл | Сейчас! |
| **QUICKSTART_WSL.txt** | Краткая шпаргалка | Первый запуск |
| **WSL_SETUP_GUIDE.md** | Полное руководство | Детальная настройка |
| **WSL_README.md** | Обзор ветки | Общая информация |
| **CHANGELOG_WSL.md** | История изменений | Для разработчиков |
| **udp_relay.py** | UDP relay скрипт | Если прямое соединение не работает |

---

## 🔧 Вспомогательные скрипты:

| Скрипт | Описание | Использование |
|--------|----------|---------------|
| **wsl_start_receiver.sh** | Автозапуск приёмника | `sudo ./wsl_start_receiver.sh` |
| **wsl_start_sender.sh** | Автозапуск отправителя | `sudo ./wsl_start_sender.sh <IP>` |
| **udp_relay.py** | UDP relay (Windows) | `python udp_relay.py` |

---

## ⚡ Быстрая проверка перед запуском:

```bash
# 1. Проверьте WSL версию (должно быть 2)
wsl --list --verbose

# 2. Проверьте зависимости
dpkg -l | grep libsodium-dev

# 3. Проверьте бинарники
ls -lh build/tap_encrypt build/tap_decrypt

# 4. Проверьте IP
hostname -I
```

Если всё ✅ - переходите к запуску!

---

## ❓ Что делать если не работает?

### 1. Прочитайте раздел "Решение проблем" в `WSL_SETUP_GUIDE.md`

### 2. Проверьте типичные проблемы:

- ❌ **"Ошибка при получении ключа"** → Проверьте брандмауэр Windows
- ❌ **"Ping не работает между WSL"** → Используйте UDP relay
- ❌ **"TAP не создаётся"** → Только WSL 2 поддерживается

### 3. Используйте отладочные команды:

```bash
# Проверка UDP порта
sudo lsof -i UDP:12346

# Мониторинг трафика
sudo tcpdump -i any -n 'udp port 12346'

# Тест связности
ping <IP_устройства_B>
echo "test" | nc -u <IP> 12346
```

---

## 🎯 Ожидаемые результаты:

После успешного запуска вы увидите:

**На приёмнике:**
```
✅ Сокет привязан к 0.0.0.0:12346 (WSL-ready mode)
📥 Публичный ключ отправителя получен от X.X.X.X:XXXXX
📤 Отправлен свой публичный ключ отправителю
🔄 Двунаправленная передача включена
```

**На отправителе:**
```
✅ UDP сокет подключён к X.X.X.X:12346
📤 Публичный ключ отправлен получателю
📥 Публичный ключ получен от получателя
🔄 Двунаправленная передача включена
```

**При ping:**
```
64 bytes from 10.0.0.2: icmp_seq=1 ttl=64 time=5.42 ms
```

**В логах программ:**
```
📤 Отправлен зашифрованный кадр (98 байт)
📥 Принят и расшифрован кадр (98 байт)
✅ Хеши совпадают — кадр корректен
```

---

## 💡 Полезные ссылки:

- [Основная документация проекта](README.md)
- [GUI версия](gui/README_GUI.md)
- [Custom Codec](CipherKeys/)

---

## 🎉 Удачного тестирования!

Если всё работает - поздравляю! У вас есть защищённый туннель между двумя Windows машинами через WSL! 🔒

**Следующие шаги:**
- Тестируйте производительность с `iperf`
- Попробуйте передачу файлов
- Используйте GUI для удобства

---

**Вопросы?** См. `WSL_SETUP_GUIDE.md` раздел "Решение проблем"

**Версия:** 2.0-wsl | **Дата:** Декабрь 2024

