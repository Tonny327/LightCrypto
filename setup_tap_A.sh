#!/bin/bash
# Настройка tap0 на компьютере A (после запуска tap_encrypt)

echo "🔧 Настройка tap0 на компьютере A..."

# Проверяем, что tap0 существует
if ! ip link show tap0 &>/dev/null; then
    echo "❌ Ошибка: tap0 не существует!"
    echo "   Сначала запустите: sudo ./build/tap_encrypt <IP_компьютера_B> 12345"
    exit 1
fi

echo "✅ tap0 найден"

# Отключаем IPv6 (чтобы не мешал)
echo "  → Отключение IPv6 на tap0..."
sudo sysctl -w net.ipv6.conf.tap0.disable_ipv6=1 >/dev/null 2>&1

# Поднимаем интерфейс
echo "  → Поднятие интерфейса tap0..."
sudo ip link set tap0 up

# Назначаем IP-адрес
echo "  → Назначение IP-адреса 10.0.0.1/24..."
sudo ip addr add 10.0.0.1/24 dev tap0 2>/dev/null

echo ""
echo "✅ Настройка завершена!"
echo ""
echo "📊 Статус tap0:"
ip addr show tap0

echo ""
echo "🧪 Можете проверить ping:"
echo "   ping 10.0.0.2"

